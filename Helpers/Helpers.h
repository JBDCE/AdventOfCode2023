#ifndef Helpers_H
#define Helpers_H

#include <string>
#include <fstream>
#include <vector>

// Function prototype
std::vector<std::string> readFile(std::string filepath);

std::string textToNumber(std::string inputString);

#endif
